import React from 'react';
import { Layout, Sections } from '../components';

// ========================
const IndexPage = () => {
  return (
    <Layout>
      <Sections />
    </Layout>
  );
};

export default IndexPage;
